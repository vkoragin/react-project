import { Layout } from '../layout/layout';
import { RestaurantPage } from '../restaurant-page/restaurant-page';

export const App = () => {
  return (
    <Layout>
      <RestaurantPage />
    </Layout>
  );
};
